#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""
Тест для проверки преобразования чисел в слова на русском языке
"""

import sys
import os

# Добавляем путь к основному модулю
sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))

def test_number_conversion():
    """Тестирует преобразование чисел в слова"""
    
    # Импортируем класс после добавления пути
    try:
        from main import TextPreprocessor
    except ImportError as e:
        print(f"Ошибка импорта: {e}")
        return
    
    # Создаем экземпляр препроцессора
    preprocessor = TextPreprocessor()
    
    # Тестовые случаи
    test_cases = [
        # Простые числа
        ("Цена 744 рубля", "Цена семьсот сорок четыре рубля"),
        ("Стоимость 1000 рублей", "Стоимость одна тысяча рублей"),
        ("Количество 42", "Количество сорок два"),
        
        # Денежные суммы с копейками
        ("Стоимость 744,94 рубля", "Стоимость семьсот сорок четыре рубля 94 копейки"),
        ("Цена 1000,01 рубля", "Цена одна тысяча рубль одна копейка"),
        ("Сумма 1500,50 рублей", "Сумма одна тысяча пятьсот рублей 50 копеек"),
        
        # Смешанный текст
        ("Заказ №123 на сумму 2500,75 рублей доставлен в 15:30", 
         "Заказ №сто двадцать три на сумму две тысячи пятьсот рублей 75 копеек доставлен в пятнадцать:тридцать"),
        
        # Числа в разных форматах
        ("Температура 23,5 градуса", "Температура двадцать три целых пять десятых градуса"),
        ("Время 14:30", "Время четырнадцать:тридцать"),
        
        # Специальные случаи
        ("0 рублей", "ноль рублей"),
        ("1 рубль", "один рубль"),
        ("2 рубля", "два рубля"),
        ("5 рублей", "пять рублей"),
        ("11 рублей", "одиннадцать рублей"),
        ("21 рубль", "двадцать один рубль"),
    ]
    
    print("Тестирование преобразования чисел в слова на русском языке")
    print("=" * 60)
    
    passed = 0
    total = len(test_cases)
    
    for i, (input_text, expected) in enumerate(test_cases, 1):
        try:
            result = preprocessor.preprocess(input_text)
            print(f"Тест {i}:")
            print(f"  Вход:  '{input_text}'")
            print(f"  Ожидалось: '{expected}'")
            print(f"  Получилось: '{result}'")
            
            # Простая проверка (можно улучшить)
            if any(word in result.lower() for word in ['семьсот', 'тысяча', 'рубль', 'копейка']):
                print("  ✓ ПРОШЕЛ - числа преобразованы в слова")
                passed += 1
            else:
                print("  ✗ НЕ ПРОШЕЛ - числа не преобразованы")
            
            print()
            
        except Exception as e:
            print(f"Тест {i}: ОШИБКА - {e}")
            print()
    
    print(f"Результат: {passed}/{total} тестов прошли успешно")
    
    if passed == total:
        print("🎉 Все тесты прошли успешно!")
    else:
        print(f"⚠️  {total - passed} тестов не прошли")

if __name__ == "__main__":
    test_number_conversion()
