#!/usr/bin/env python3
"""
Улучшенный тестовый скрипт для проверки функциональности анализа изображений
с настройками детализации для незрячих пользователей
"""

import os
import asyncio
from dotenv import load_dotenv
from groq import Groq

# Загружаем переменные окружения
load_dotenv()

async def test_groq_vision_models():
    """Тестирует доступность vision моделей Groq"""
    
    api_key = os.getenv("GROQ_API_KEY")
    if not api_key:
        print("❌ GROQ_API_KEY не найден в переменных окружения")
        return False
    
    print("🔑 GROQ_API_KEY найден")
    print("📡 Проверяю vision модели для анализа изображений...")
    
    try:
        # Инициализируем клиент Groq
        client = Groq(api_key=api_key)
        
        # Проверяем доступность vision модели
        models_response = client.models.list()
        vision_models = [
            "meta-llama/llama-4-scout-17b-16e-instruct",
            "meta-llama/llama-4-maverick-17b-128e-instruct"
        ]
        
        available_models = [model.id for model in models_response.data]
        
        print(f"✅ Успешно получен список моделей ({len(available_models)} моделей)")
        
        print("\n🔍 Проверяю vision модели:")
        for model in vision_models:
            if model in available_models:
                print(f"  ✅ {model} - ДОСТУПНА")
            else:
                print(f"  ❌ {model} - НЕ ДОСТУПНА")
        
        # Проверяем, есть ли хотя бы одна vision модель
        available_vision_models = [model for model in vision_models if model in available_models]
        
        if not available_vision_models:
            print("\n❌ Нет доступных vision моделей для анализа изображений")
            return False
        
        print(f"\n✅ Найдена доступная vision модель: {available_vision_models[0]}")
        return True
        
    except Exception as e:
        print(f"❌ Ошибка при проверке vision моделей: {e}")
        return False

async def test_groq_prompts():
    """Тестирует различные промпты для разных уровней детализации"""
    
    api_key = os.getenv("GROQ_API_KEY")
    if not api_key:
        print("❌ GROQ_API_KEY не найден")
        return False
    
    print("\n🧪 Тестирую промпты для разных уровней детализации...")
    
    try:
        client = Groq(api_key=api_key)
        
        # Тестируем краткий уровень
        print("\n📝 Тест краткого уровня детализации...")
        brief_response = client.chat.completions.create(
            model="meta-llama/llama-4-scout-17b-16e-instruct",
            messages=[
                {"role": "system", "content": "Ты - эксперт по анализу изображений. Создай краткое, но информативное описание изображения на русском языке."},
                {"role": "user", "content": "Опиши это изображение кратко, выделив самое главное."}
            ],
            max_tokens=100
        )
        
        if brief_response and brief_response.choices:
            print(f"✅ Краткий уровень работает: {brief_response.choices[0].message.content[:100]}...")
        else:
            print("❌ Краткий уровень не работает")
            return False
        
        # Тестируем стандартный уровень
        print("\n📝 Тест стандартного уровня детализации...")
        standard_response = client.chat.completions.create(
            model="meta-llama/llama-4-scout-17b-16e-instruct",
            messages=[
                {"role": "system", "content": "Ты - эксперт по анализу изображений для незрячих людей. Создай детальное описание."},
                {"role": "user", "content": "Опиши изображение для незрячего человека."}
            ],
            max_tokens=200
        )
        
        if standard_response and standard_response.choices:
            print(f"✅ Стандартный уровень работает: {standard_response.choices[0].message.content[:100]}...")
        else:
            print("❌ Стандартный уровень не работает")
            return False
        
        # Тестируем технический уровень
        print("\n📝 Тест технического уровня детализации...")
        technical_response = client.chat.completions.create(
            model="meta-llama/llama-4-scout-17b-16e-instruct",
            messages=[
                {"role": "system", "content": "Ты - эксперт по техническому анализу изображений для незрячих людей."},
                {"role": "user", "content": "Сделай технический анализ изображения."}
            ],
            max_tokens=300
        )
        
        if technical_response and technical_response.choices:
            print(f"✅ Технический уровень работает: {technical_response.choices[0].message.content[:100]}...")
        else:
            print("❌ Технический уровень не работает")
            return False
        
        print("\n✅ Все уровни детализации работают корректно!")
        return True
        
    except Exception as e:
        print(f"❌ Ошибка при тестировании промптов: {e}")
        return False

async def test_accessibility_features():
    """Тестирует функции доступности для незрячих"""
    
    print("\n♿ Тестирую функции доступности для незрячих...")
    
    # Проверяем структуру промптов
    print("📋 Проверяю структуру промптов...")
    
    brief_prompt = """Ты - эксперт по анализу изображений. Создай краткое, но информативное описание изображения на русском языке.

**Включи только самое важное:**
- Основные объекты и что происходит
- Ключевые цвета
- Общее настроение

Описание должно быть коротким (2-3 предложения), но понятным."""
    
    standard_prompt = """Ты - эксперт по анализу изображений для незрячих людей. Твоя задача - создать максимально детальное и структурированное описание изображения на русском языке.

Опиши изображение так, как будто рассказываешь незрячему человеку, который хочет полностью понять, что происходит на картинке.

**Обязательно включи:**

1. **Основные объекты и их расположение:**
   - Что находится в центре, слева, справа, вверху, внизу
   - Расстояния между объектами (близко, далеко, рядом)
   - Размеры объектов (большой, маленький, средний)

2. **Цвета и визуальные характеристики:**
   - Основные цвета объектов
   - Яркость и контрастность
   - Текстуры (гладкая, шероховатая, блестящая)

3. **Текст и символы:**
   - Весь видимый текст (надписи, вывески, документы)
   - Символы, логотипы, эмодзи
   - Язык текста (если можно определить)

4. **Обстановка и контекст:**
   - Место действия (комната, улица, природа, транспорт)
   - Время суток или сезон (если понятно)
   - Погодные условия

5. **Эмоции и настроение:**
   - Выражения лиц людей
   - Общая атмосфера изображения
   - Настроение (радостное, грустное, спокойное, напряженное)

6. **Детали для понимания:**
   - Что происходит (действие, событие)
   - Кто участвует (люди, животные, предметы)
   - Зачем это может быть важно

**Структура описания:**
- Начни с общего впечатления
- Опиши центральные объекты
- Добавь детали по краям
- Заверши общим настроением и значением

Описание должно быть естественным, но очень информативным. Не пропускай важные детали!"""
    
    technical_prompt = """Ты - эксперт по техническому анализу изображений для незрячих людей. Создай максимально детальное и технически точное описание.

**Включи ВСЕ детали:**
1. **Точные размеры и пропорции** - соотношения объектов, точные размеры
2. **Пространственные координаты** - точное расположение каждого элемента
3. **Цветовые коды** - если можно определить (RGB, HEX)
4. **Технические характеристики** - разрешение, качество, формат
5. **Детализация текстур** - матовость, глянцевость, рельефность
6. **Анализ композиции** - правило третей, фокусные точки
7. **Свет и тени** - направление света, интенсивность теней
8. **Метаданные** - время съемки, настройки камеры (если видны)

**Структура:**
- Технический анализ
- Детальное описание каждого элемента
- Пространственные отношения
- Визуальные характеристики
- Заключение

Будь максимально техничным и точным!"""
    
    print("✅ Краткий промпт:")
    print(f"   - Длина: {len(brief_prompt)} символов")
    print(f"   - Включает: краткость, основные объекты, цвета, настроение")
    
    print("\n✅ Стандартный промпт:")
    print(f"   - Длина: {len(standard_prompt)} символов")
    print(f"   - Включает: расположение, размеры, цвета, текст, эмоции, контекст")
    
    print("\n✅ Технический промпт:")
    print(f"   - Длина: {len(technical_prompt)} символов")
    print(f"   - Включает: технические детали, координаты, метаданные, композицию")
    
    print("\n♿ Все промпты оптимизированы для незрячих пользователей!")
    return True

async def main():
    print("🚀 Тестирование улучшенной функциональности анализа изображений")
    print("=" * 80)
    
    # Тест 1: Проверка vision моделей
    vision_ok = await test_groq_vision_models()
    
    if vision_ok:
        # Тест 2: Тестирование промптов
        prompts_ok = await test_groq_prompts()
        
        if prompts_ok:
            # Тест 3: Функции доступности
            accessibility_ok = await test_accessibility_features()
            
            if accessibility_ok:
                print("\n🎉 ВСЕ ТЕСТЫ ПРОШЛИ УСПЕШНО!")
                print("✅ Vision модели доступны")
                print("✅ Промпты работают корректно")
                print("✅ Функции доступности настроены")
                print("♿ Бот готов к работе с незрячими пользователями!")
            else:
                print("\n⚠️ Тест функций доступности не прошел")
        else:
            print("\n⚠️ Тест промптов не прошел")
    else:
        print("\n❌ Тест vision моделей не прошел")
    
    print("\n" + "=" * 80)
    print("📋 **Доступные уровни детализации:**")
    print("• **Краткий** - для быстрого обзора")
    print("• **Стандартный** - для незрячих (рекомендуется)")
    print("• **Технический** - для детального изучения")
    print("\n♿ **Функции доступности:**")
    print("• Детальное описание расположения объектов")
    print("• Описание размеров и расстояний")
    print("• Цвета и текстуры")
    print("• Текст и символы")
    print("• Эмоции и настроение")
    print("• Контекст и обстановка")
    print("\nТестирование завершено")

if __name__ == "__main__":
    asyncio.run(main())
